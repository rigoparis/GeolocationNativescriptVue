// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.ciriscr.geotest"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
  project.ext {
    googlePlayServicesVersion = "12.0.1"
  }
}
